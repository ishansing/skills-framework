# Golden Case Result - 2026-09-16

Case: `add-collaborator-invitations` (`tests/skills/golden/add-collaborator-invitations.md`)
run in `~/itp-golden-app`, a separate repo where framework v2.0.0 was installed by
`install.sh`. One human answered the alignment interview.

Verdict: **pass**.

| Expectation | Observed |
|---|---|
| required adapters/children | align, spec, architecture, slice, implement, tdd, review, code-review, verify, verification-before-completion all evidenced by artifacts and the run ledger |
| `research` phase | skipped with a recorded reason (no external question); allowed by the routing rules, and not in the golden required list |
| conditional trigger | `differential-review` classified the access-control diff HIGH and produced `docs/work/differential-review.md`; no critical/high vectors |
| forbidden | no runtime skill install (sandbox still has exactly the 12 framework skills); no recursive `to-spec` |
| artifacts | `spec.md`, `acceptance-criteria.md` (14 `AC-` refs), `architecture.md`, `ADR/0001-*`, `ISSUE-1.md`, `review.md`, `verification.md`, `.itp/run.md`, plus `decisions.md`, `CONTEXT.md` |
| review before verify | `review.md` verdict **approved**, `recommended_next: itp-verify`; `verification.md` verdict **pass** with in-session evidence |
| fresh evidence | `npm test` re-run here after the fact: 9 tests, 9 pass, exit 0 |
| user-entry handling | `setup-matt-pocock-skills` was loaded only after an explicit user instruction recorded as a resolved handoff (created `docs/agents/*` + an AGENTS.md block) |
| ledger | `next_phase: complete`, completed phases listed, evidence recorded, open questions empty |

## Deviations observed

- The session's skill-tool output was mangled by the local caveman compression layer
  (unrecoverable handles), so the agent read adapter and upstream `SKILL.md` files directly
  from disk. Framework behavior was unaffected because skills are plain files; worth knowing
  for future validation sessions.
- `AGENTS.md` in the sandbox gained the setup-matt-pocock-skills block, as designed.

## Still open for section 25

- adapter isolation for align/architecture/slice/implement/review/incident outside the golden
  path, composition chains, recursion bounds, missing-skill handoff, and a second golden run
  to show repeatability rather than a single pass.

Artifacts live in `~/itp-golden-app` (separate repo, keep or delete).
